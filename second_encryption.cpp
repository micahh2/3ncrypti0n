#include "ecryption2.h"

int character=//number of unicode character to use,
length=9//max length of string to represent character, 
string_num=//number representing strings of that character, 
encryption_type=1//unicode encryption;

void generate_key(){
	string val;
	for(int k=0; k<character; k++){
		for(int j=0;j<length;j++){
			for(i=0; i<string_num;i++){

				while(search(generate_value(encryption_type),j)!=1);
				//else
				value.push_back(val);
				//or
				
				//myfile << val;
				}
				string_length.push_back(value);
		}
		character.push_back(string_length);
		//character.push_back(string_length.pushback(value));
	}
}